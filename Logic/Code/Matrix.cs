using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Security.Principal;
using System.Text;

namespace Logic.Code
{
    internal class Matrix
    {
        //106. Matrix Addition → add matching cells
        //107. Matrix Subtraction → subtract matching cells
        //108. Matrix Multiplication → row × column
        //109. Transpose → swap rows and columns
        //110.Diagonal Sum → arr[i][i]
        //111. Upper Triangle → j>=i
        //112. Lower Triangle → j<=i
        //113. Row Sum → sum each row
        //114. Column Sum → sum each column
        //115. Identity Matrix → diagonal=1, rest=0
        //116. Symmetric → matrix==transpose
        //117. Sparse → mostly zeros
        //118.Rotate 90° → transpose + reverse rows
        //119.Rotate 180° → reverse rows and cols
        //120.Rotate 270° → transpose + reverse cols
    }
}
