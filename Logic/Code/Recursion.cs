using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Text;

namespace Logic.Code
{
    internal class Recursion
    {
        //121. Factorial → n* factorial(n-1)
        //122. Fibonacci → f(n-1)+f(n-2)
        //123. Sum N → n + sum(n-1)
        //124. Power → n* power(n-1)
        //125. Reverse String → process last char first
        //126.Palindrome → compare ends recursively
        //127. Sum Digits → digit + recurse(rest)
        //128. Binary Search → recurse left/right half
        //129.Tower of Hanoi → move n-1 disks
        //130. Subsets → include/exclude element
    }
}
