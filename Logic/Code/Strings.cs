using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using System.Text;

namespace Logic.Code
{
    internal class Strings
    {
        //51. Reverse String → swap chars
        //52.Palindrome String → reverse and compare
        //53. Count Vowels → check a, e, i, o, u
        //54.Count Consonants → letters not vowels
        //55. Count Words → split by spaces
        //56. Frequency → dictionary count
        //57.Remove Duplicates → HashSet
        //58. Remove Spaces → replace/remove
        //59. Count Uppercase → char.IsUpper
        //60. Count Lowercase → char.IsLower
        //61. Count Digits → char.IsDigit
        //62. First Non-Repeated → frequency then find count = 1
        //63. First Repeated → track seen chars
        //64. Anagram → sort and compare
        //65. String Compression → count consecutive chars
        //66. Reverse Words → split, reverse words
        //67.Longest Word → split and find max length
        //68. Replace Character → string.Replace
        //69. Toggle Case → upper↔lower
        //70. Digits Only → every char digit
    }
}
