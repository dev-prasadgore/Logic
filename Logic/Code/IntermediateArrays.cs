using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Globalization;
using System.Text;

namespace Logic.Code
{
    internal class IntermediateArrays
    {
        //91. Bubble Sort → adjacent swaps
        //92.Selection Sort → select minimum each pass
        //93.Insertion Sort → insert into sorted part
        //94.Quick Sort → partition around pivot
        //95. Merge Sort → divide and merge
        //96. Move Zeros End → move non-zeros first
        //97. Move Zeros Start → move zeros first
        //98. Pair Sum → HashSet lookup
        //99.Largest Difference → max-min
        //100. Majority Element → count > n/2
        //101. Intersection → common elements
        //102.Union → combine unique elements
        //103. Rotate K Times → repeated rotation/modulo
        //104. Kadane → max subarray sum
        //105. Max Product Pair → two largest numbers
    }
}
